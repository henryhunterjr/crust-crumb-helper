// Krusty Skool Helper — background service worker
// Currently a no-op placeholder; reserved for future queue/throttle and licensing.
chrome.runtime.onInstalled.addListener(() => {
  console.log('[Krusty] background worker installed');
});