{
  "_note": "memberCard re-derived 2026-07-01 after a Skool rebuild dropped the old 'MemberItemWrapper' styled-component name. Skool now ships fully-hashed class names with NO stable semantic prefix, so this selector is a build hash and WILL break again on Skool's next rebuild. To re-derive when a read returns 0 members: sign the roster-agent-profile Chrome into Skool (visible), then run diag2.mjs (finds the class token whose docCount equals members-per-page, ~30) and diag3.mjs (verifies clean name/username/join extraction), and paste the winning token here. profileLink and loggedOutMarker are semantic and stable.",

  "loggedOutMarker": "input[type='password'], a[href*='/login']:not([href*='crust-crumb'])",

  "memberCard": "div[class*='sc-db9ce526-0']",

  "profileLink": "a[href^='/@']",

  "pagination": {
    "mode": "numbered-next",
    "nextButtonName": "Next"
  }
}
